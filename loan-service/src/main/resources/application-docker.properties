server.port=8083

spring.application.name=loan-service

# MySQL Database (DOCKER)
spring.datasource.url=jdbc:mysql://mysql:3306/lms_loan_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Riwpc@1104
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Internal service URLs (DOCKER service names)
customer.service.url=http://customer-service:8082

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always