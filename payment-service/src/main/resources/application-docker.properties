server.port=8084

spring.application.name=payment-service

# MySQL Database (DOCKER)
spring.datasource.url=jdbc:mysql://mysql:3306/lms_payment_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Riwpc@1104
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Internal service URLs (DOCKER)
loan.service.url=http://loan-service:8083
customer.service.url=http://customer-service:8082

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always